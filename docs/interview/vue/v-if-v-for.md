# v-if/v-for

## vue2 中

- 同一个元素上同时使用这两个指令，v-for 的优先级高于 v-if。
- 编译器会将 v-for 处理在先。每次渲染时，Vue 会先遍历整个列表，然后再对每一个项进行 v-if 条件判断。会带来性能损耗。

## vue3 中

- 由于 v-if 先执行，此时 v-for 循环尚未开始，因此 v-if 的逻辑中无法访问到 v-for 定义的临时变量（item）。会带来作用域报错。

## 建议

使用`computed`计算属性来过滤列表数据，然后在 v-for 中遍历过滤后的数据。
