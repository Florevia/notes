# 负载均衡

负载均衡（Load Balance）就是 **“流量调度员”**。

它的核心作用是将大量的并发请求，按照一定的规则分发给多台后端服务器。

1. 高可用性：这是最重要的。当某台服务器宕机时，负载均衡器能自动剔除故障节点，将 **流量** 转给健康的服务器，保证服务不中断。

2. 横向扩展：当业务量暴增（比如双11），我们只需要 **加服务器，负载均衡器就能自动利用新资源，而无需修改前端代码或域名配置**。

3. 安全性：隐藏了后端真实服务器的 IP，对外只暴露负载均衡器的虚拟 IP，构建了第一道安全屏障。
