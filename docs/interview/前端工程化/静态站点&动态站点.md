# 静态站点 & 动态站点

## 静态站点

- 核心机制： 构建时生成（Build Time）
- 定义：所有的 HTML 文件在部署前（构建阶段）就已经生成完毕。
- 特点：
  - 性能： 极快，可通过 CDN 分发，无需服务器运算。
  - 安全性： 无数据库连接，攻击面小。

## 动态站点

- 核心机制： 运行时生成（Run Time）
- 定义：页面内容是根据用户的请求、身份、时间等因素，在服务器端（SSR）或客户端（CSR）动态生成的。
- 特点：
  - 个性化
  - 数据实时性
  - 架构： 通常需要服务器和数据库支持
